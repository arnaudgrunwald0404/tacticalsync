# Required (Socket Mode)
SLACK_APP_TOKEN={{xapp-...}}           # App-level token with connections:write
SLACK_BOT_TOKEN={{xoxb-...}}           # Bot token
SLACK_SIGNING_SECRET={{...}}           # Signing secret

# Slack scopes needed (add in OAuth & Permissions):
# - commands, chat:write, app_mentions:read
# - users:read.email (to map Slack user -> Supabase profile by email)

# Supabase (server-side access)
VITE_SUPABASE_URL={{https://YOUR-PROJECT.ref.supabase.co}}
SUPABASE_SERVICE_ROLE_KEY={{YOUR_SERVICE_ROLE_KEY}}

# Optional/Recommended
SLACK_CLIENT_ID={{...}}
SLACK_CLIENT_SECRET={{...}}

# Posting targets
SLACK_DEFAULT_CHANNEL_ID={{C...}}      # Primary channel for posts
SLACK_TEST_CHANNEL_ID=C09V10M4NDN      # Your test channel
SLACK_SCHEDULE_DM_USER_ID=U070XL0K16K     # Optional: a Slack user ID (U...) to DM on schedule

# Scheduling (set CRON to enable)
# Example: 0 9 * * 1-5 -> 09:00 Mon-Fri (see https://crontab.guru)
# Example (dev): */5 * * * * -> every 5 minutes
SLACK_SCHEDULE_CRON=
SLACK_SCHEDULE_TEXT=Reminder: Daily check-in. Use /checkin to submit.

# Timezone for scheduler (defaults to UTC if unset)
TZ=UTC
